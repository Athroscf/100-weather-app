import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '100-weather-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
